create table "text"
(
	item_id integer,
	entity character(30),
	enumerator integer,
	attribute character(65),
	text text,
	item_year character(4),
	item_month character(2),
	item_date date,
	text_key integer
);
